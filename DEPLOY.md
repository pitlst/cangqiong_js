# 苍穹「配置项-新页面」填报面板 —— 部署与联调指南（纯前端版）

> 基于官方「前端页面脚本扩展」的 DOM 驱动能力实现，**无需服务端插件**。
> 新增/修改/删除全部通过驱动官方按钮完成，不伪造数据。
> 知识库依据：《前端页面脚本扩展》 https://vip.kingdee.com/knowledge/588370901079337984

## 已确认的环境信息（2026-08-13 实测）

| 项 | 值 |
|---|---|
| 系统地址 | https://cangqiongtestzelc.crrcgc.cc:6888/ierp |
| 目标单据 | 配置项-新页面，编码 **`crrc_dj_config_new`**，BillFormModel（单据） |
| 主字段 | 单据编号(billno)、单据状态(billstatus)、创建人、审核人、**配置json**（必录文本） |
| 子表 | 「配置项应用的党组织」（列：党组织） |
| 现有插件 | TemplateBillEdit, CodeRulePlugin（无需改动、无需新增） |

## 工作原理

```
列表页（列表表单模板设计器 JS）              单据页（单据布局设计器 JS）
┌──────────────────────────┐    localStorage 待办   ┌──────────────────────────┐
│ 面板：读官方列表 DOM 展示   │ ─── __cq_pending__ ──▶ │ 检测待办 → 官方 setValue 填充 │
│ 新增 → 点官方「新增」按钮   │                       │ → 可选自动点官方「保存」      │
│ 修改 → 双击行打开官方单据   │                       │ 迷你助手：[填充][点官方保存]  │
│ 删除 → 勾选→官方删除→点确定 │                       └──────────────────────────┘
└──────────────────────────┘
```

- 跨页签通信用 `localStorage`（同源共享），待办 10 分钟自动过期。
- 字段填充走官方 API `this.$('字段标识').setValue()`（知识库第五章），懒加载用 `wait()`（第十二章 FAQ2）。
- 按钮点击走 DOM 驱动（知识库第九章案例 5/6 同款思路）。

## 部署前：只需填 1 个必填项

| 占位符 | 位置 | 对应对象 | 获取方法 |
|---|---|---|---|
| `TODO_配置json字段标识` | `CONFIG.configFieldKey` 和 `CONFIG.newBillDefaults` 的 key | 主字段「配置json」 | 单据布局设计器 → 大纲「显示标识」→ 配置json 字段的标识 |

其余都是可选项：`newBillDefaults` 的 value 是新增默认值；`autoSaveAfterFill` 控制填充后是否自动点保存（建议先 false）。

## 部署步骤（同一份脚本贴两处）

1. **单据布局**：打开你已进入的设计器（配置项-新页面，byPageId=root13f4b148...）→ 顶部 **JS** → 全选替换为 `page-script.js` → Ctrl+S → **再保存一次设计器**。
2. **列表页**：配置项列表的「列表表单模板」设计器 → **JS** → 同样粘贴 `page-script.js` → Ctrl+S → 保存设计器。
3. `server-plugin.ks` 可忽略（本方案不需要服务端插件；如后续想走 fetchData 完整 CRUD 再启用）。

## 验证

1. 打开配置项列表 → 面板应列出列表数据（状态列显示徽章）。
2. 面板点「新增」→ 官方新增页自动打开 → 新页自动填充默认值（有提示）。
3. 面板编辑「配置json」单元格 → 点「修改选中行」→ 官方单据页打开 → 自动填充编辑值 → 确认后点官方保存。
4. 面板选中行点「删除」→ 官方删除并自动点确认框。
5. 调试：F12 控制台执行 `window.show_ps_init = true` 刷新看脚本加载日志；面板操作日志前缀 `[cq-fill]`。

## 常见问题排查

| 现象 | 排查方向 |
|---|---|
| 面板不出现 | `show_ps_init` 无日志 → 脚本没保存/没发布；有日志报错 → 看 Console |
| 点「新增」没反应 | 官方「新增」按钮文本与 CONFIG.toolbarText.newBtn 不一致 → 按界面实际文本改 |
| 新页没自动填充 | `configFieldKey` 还是 TODO → 填真实标识后重新保存 |
| 删除没自动确认 | 确认框按钮文本不在 dialogOkText → 按实际文本（确定/是）补充 |
| 双击行没打开单据 | 官方列表可能是单击/按钮打开 → 手动打开后，单据页助手可点「填充配置json」手动填充 |
| 修改/新增后列表没刷新 | 面板点「刷新」重读 DOM；或切换页签后点刷新 |

## 注意事项

- DOM 驱动依赖官方按钮文本与页面结构，**苍穹升级后需回归验证**（官方文档明示 DOM 结构不保证稳定）。
- 面板的「修改」只同步「配置json」列；组织子表等仍在官方页面维护。
- 已提交/已审核（状态 B/C）的行，面板禁止发起修改（CONFIG.lockedStatus）。
- 脚本里所有回调用箭头函数（官方要求）；全量 try/catch 避免未捕获异常中断脚本（官方注意事项）。
