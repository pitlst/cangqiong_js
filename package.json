{
  "name": "cangqiong-config-panel",
  "private": true,
  "scripts": {
    "build:tables": "node build-tables.mjs",
    "embed": "node embed-module.js",
    "build": "npm run build:tables && npm run embed"
  },
  "dependencies": {
    "@tanstack/react-table": "8.20.5",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "esbuild": "0.25.0"
  }
}
